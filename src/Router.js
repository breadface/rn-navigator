import React, { Component } from 'react'
import { View, Text } from 'react-native'

class Router extends Component {
  render(){
    return (
      <View>
        <Text>Basic react native router</Text>
      </View>
    )
  }
}

export default Router
